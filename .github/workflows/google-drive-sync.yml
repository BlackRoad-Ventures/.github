# Google Drive Sync via rclone on Pi fleet
# Syncs critical BlackRoad files to Google Drive every 24h
# Cost: $0 — runs on self-hosted Pi runner
name: "☁️ Google Drive Sync"

on:
  schedule:
    - cron: '0 6 * * *'   # 6 AM UTC daily
  workflow_dispatch:
    inputs:
      full_sync:
        description: 'Full sync (not incremental)'
        type: boolean
        default: false

concurrency:
  group: gdrive-sync
  cancel-in-progress: true

jobs:
  sync-to-drive:
    name: "📁 Sync to Google Drive"
    runs-on: [self-hosted, blackroad-fleet]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Check rclone installed
        run: |
          which rclone && rclone --version | head -1 || {
            echo "Installing rclone..."
            curl -fsSL https://rclone.org/install.sh | sudo bash 2>/dev/null || \
            sudo apt-get install -y rclone 2>/dev/null || \
            echo "⚠️ rclone not installed — configure manually"
          }

      - name: Configure rclone
        env:
          RCLONE_CONFIG_B64: ${{ secrets.RCLONE_CONFIG }}
        run: |
          mkdir -p ~/.config/rclone
          if [ -n "$RCLONE_CONFIG_B64" ]; then
            echo "$RCLONE_CONFIG_B64" | base64 -d > ~/.config/rclone/rclone.conf
            echo "✓ rclone configured from secret"
          elif [ -f ~/.config/rclone/rclone.conf ]; then
            echo "✓ rclone using existing config"
          else
            echo "⚠️ No rclone config — skipping sync"
          fi

      - name: Sync memory/ to Google Drive
        run: |
          rclone sync memory/ gdrive:BlackRoad/memory/ \
            --exclude ".git/**" \
            --progress --stats-one-line \
            2>/dev/null && echo "✓ memory synced" || echo "⚠️ memory sync failed"
        continue-on-error: true

      - name: Sync agents/ to Google Drive
        run: |
          rclone sync agents/ gdrive:BlackRoad/agents/ \
            --exclude ".git/**" \
            --progress --stats-one-line \
            2>/dev/null && echo "✓ agents synced" || echo "⚠️ agents sync failed"
        continue-on-error: true

      - name: Sync coordination/ to Google Drive
        run: |
          rclone sync coordination/ gdrive:BlackRoad/coordination/ \
            --exclude ".git/**" \
            --progress --stats-one-line \
            2>/dev/null && echo "✓ coordination synced" || echo "⚠️ coordination sync failed"
        continue-on-error: true

      - name: Sync tools/ to Google Drive
        run: |
          rclone sync tools/ gdrive:BlackRoad/tools/ \
            --exclude ".git/**" --exclude "node_modules/**" \
            --progress --stats-one-line \
            2>/dev/null && echo "✓ tools synced" || echo "⚠️ tools sync failed"
        continue-on-error: true

      - name: Sync blackroad-sf/ to Google Drive
        run: |
          rclone sync blackroad-sf/ gdrive:BlackRoad/blackroad-sf/ \
            --exclude ".git/**" --exclude "node_modules/**" \
            --progress --stats-one-line \
            2>/dev/null && echo "✓ blackroad-sf synced" || echo "⚠️ sf sync failed"
        continue-on-error: true

      - name: Sync docs/ to Google Drive
        run: |
          rclone sync docs/ gdrive:BlackRoad/docs/ \
            2>/dev/null && echo "✓ docs synced" || true
          # Also sync root markdown files
          for f in *.md; do
            rclone copy "$f" gdrive:BlackRoad/root-docs/ 2>/dev/null || true
          done
          echo "✓ docs synced"
        continue-on-error: true

      - name: Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "☁️  Google Drive Sync Complete: $(date -u)"
          echo "📁 Destination: gdrive:BlackRoad/"
          echo "📂 Synced: memory, agents, coordination, tools, sf, docs"
          echo "🔒 Excluded: .git, node_modules, vault (secrets stay local)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: Re-queue in 24h+3s
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.full_sync == 'true'
        run: |
          sleep 86400
          sleep 3
          curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/google-drive-sync.yml/dispatches" \
            -d '{"ref":"master"}' && echo "✅ Next sync queued" || true
